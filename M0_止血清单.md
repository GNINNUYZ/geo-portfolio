# M0 止血 — 清单与进度

> 阶段：M0（前置止血）。目标：**修掉已知缺陷 + 给后续所有实验建立验证手段**。
> 预算：20-30h。原定 2026.09 收尾。
> 纪律：本阶段**不写新功能**，只修 + 加测试 + 统一命名。

## 一、唯一确证的运行级缺陷（必修）

| # | 文件 | 位置 | 现象 | 状态 |
|---|---|---|---|---|
| 1 | `07-Voxel_filter/voxel_filter.py` | L15 定义 / **L37 使用** | `n_voxels` 在函数内定义，`__main__` 里引用 → `NameError`，脚本跑不完 | ☐ |

## 二、逻辑缺陷（必修）

| # | 文件 | 位置 | 现象 | 状态 |
|---|---|---|---|---|
| 2 | `02-halfedge-engine/halfedge.py` | `vertices_faces`（约 L47-65） | 反向遍历只走单侧，起点 face 会被重复插入；边界点返回的 face 数不可信 | ☐ |
| 3 | `02-halfedge-engine/halfedge.py` | `walk_vertices`（约 L146-159） | 遇到 `twin is None` 即 break，**只走一个方向** → 对开网格（带边界）的 2-manifold 判定失效 | ☐ |
| 4 | `01-urban-density/Amsterdam_architecture_density.py` | 约 L41-47 | ✅ **2026-09-22 已修**：scheme 改为遍历变量，`schemes` 死变量消除，标题改回 scheme 名。实测三方案分档显著不同（`equal_interval` vs `quantiles` 有 46.8% 街区落在不同档；数据 51.4% 零值、偏度 2.04）。已提交 `84ed24b` | ✅ |

## 三、验收手段（必修）

| # | 项 | 现象 | 状态 |
|---|---|---|---|
| 5 | `07-Voxel_filter/icp.py` | 收敛误差用全量源点减最近邻点、且是残差平方和未归一化 → **改为输出 RMSE**，并打印迭代数与耗时 | ☐ |
| 6 | 新建 `tests/` | 仓库无任何测试。M0 建立，之后每月维护 | ☐ |
| 7 | `tools/icp_real_test.py`（或同级） | ICP 目前只在 1000 点合成数据上验证过；须在真实 LAZ（10 万点级）上跑出**可复现的 RMSE 与耗时** | ☐ |

## 四、工程卫生（建议，非阻塞）

| # | 项 | 现象 | 状态 |
|---|---|---|---|
| 8 | `requirements.txt` | 只列了 numpy/shapely/matplotlib/pyproj；实际用到 laspy、rasterio、scipy、geopandas 等 → 补全 | ☐ |
| 9 | 目录与计划命名 | 计划写 `07-pointcloud-tool`，实际目录是 `07-Voxel_filter` → 统一（改目录名或改计划） | ☐ |
| 10 | 空文件 / 残留 stub | `03-terrain-pipeline/pca_normal.py`（M1 的交付物，暂时保留）、`practice/voxelize_mesh.py`、`practice/footprint_to_soild.py` 等 | ☐ |
| 11 | 文件名 `#` 前缀 | `#cityJSON to 3DTiles.py`、`#P1_Amsterdam_analysis.py`、`data/#体素滤波.py` | ☐ |

## 五、M0 完成判据

```
1. `python voxel_filter.py` 零报错跑完
2. `Amsterdam_architecture_density.py` 三张图用三种不同 scheme
3. `halfedge.py` 对带边界的开网格给出正确的 2-manifold 判定
4. `icp.py` 在真实 LAZ 上打印 RMSE / 迭代数 / 耗时
5. `tests/` 下至少 3 个测试能跑，且至少 1 个是"已知变换 + 加噪 + 加离群点"的回归测试
6. 无空文件残留、无 `#` 前缀文件名
```

## 六、时间现实（2026-09-21 重排：哪些 9 月做，哪些 10 月做）

```
9 月剩余可投入（9/21-9/30）：
  9/21-9/25 五个工作日 × 6h   = 30h
  9/26 周六上午                =  3h
  9/28-9/30 三个工作日 × 6h   = 18h
  ─────────────────────────────────
  合计 ≈ 51h（9/27 周日休息）

拆法：
  保命项（9 月做，约 15h）→ 做完后剩余 ~36h 提前开始线代
  卫生项（10 月做，约 6h）→ 并入 M0.5 数学轮的每天早 1h
```

**保命项（9 月，必须做完）**

| # | 项 |
|---|---|
| 1 | `voxel_filter.py` 的 `n_voxels` NameError |
| 4 | `Amsterdam_architecture_density.py` 三张相同的图 |
| 5 | `icp.py` 改输出 RMSE |
| 6 | 建 `tests/` |
| 7 | 真实 LAZ 上跑通 ICP，记录 RMSE / 耗时 |

**卫生项（10 月，并入 M0.5 早 1h）**

| # | 项 |
|---|---|
| 2 | `halfedge.py` 的 `vertices_faces` |
| 3 | `halfedge.py` 的 `walk_vertices` |
| 8 | `requirements.txt` 补全 |
| 9 | 目录命名统一（`07-Voxel_filter` vs 计划写的 `07-pointcloud-tool`） |
| 10 | 空文件 / 残留 stub 清理 |
| 11 | 三个 `#` 前缀文件名 |

> 理由：卫生项属支撑层的"工程"格，按 §5 支撑层口径本就该占早 1h；而 9 月剩余时间如果全用来做卫生，会把数学起步拖后。
> ⚠️ **2026-09-21 更正**：**M1 启动时间是 2026.10 中下旬**（不是 2026.11——那是已被撤销的"130h 大纲版"日期）。且 §4.0bis 的 M0.5 已缩为 **12-20h 补丁**，**明令"✗ 不补向量空间 / 基 / 线性无关 / 秩 / 四个基本子空间，写了就是违约"**。故下方 9/28-9/30 的原安排已作废并重写。

**M0 卫生项另含附录 A 四项待修**（2026-09-21 补入；**同日升级为"9/23 前必做"**）

> **为什么升级**：动机信已在 2026-09-21 加入仓库地址（`The code for my geo-portfolio at github.com/GINNUYZ/geo-portfolio.`，已设为超链接）。
> **于是产生一条依赖：链接已加 → 仓库必须在 9/23 英语读者轮之前"经得起打开"**，否则读者点进去看到缺口，反而减分。
> 你 2020 年 PoliMi 那次正是"作品被看见"抵消了 GPA——**反过来也成立：作品有洞被看见，会反向抵消。**

**按"对链接的伤害程度"排序（不是按原编号）**

| 优先级 | 项 | 现状（2026-09-21 实测） | 为什么这个优先级 |
|---|---|---|---|
| **1** | **P8 缺训练记录** | ✅ **2026-09-22 已解决**：给 `pointnet.py` 加落盘日志后重跑 200 epoch，得 `train_log.txt`（逐 epoch + 汇总）与 `pointnet_acc.png`。**best acc 0.8679 @ epoch 43，末轮 0.8497，总耗时 2692.96 s**。旧图 `figure_training_curve.png` 已删除（被新图取代） | 已从"图上目视"升级为"逐 epoch 可核实"——评估标准第 6 条达成。⚠️ README 已改写：**86% 是 best 不是末值** |
| **2** | **P5 `output.city.json` 缺 `metadata` + `transform`** | **已实测确认**：顶层键只有 `type, version, CityObjects, vertices`，两者皆无 | CityJSON 1.1 规范要求，**确定不合规**，最容易修 |
| **3** | **P4 `transform.scale=[1,1,1]`** | **实测：`out.json` 里 `transform` 和 `metadata` 都在**（`{"scale":[1,1,1],"translate":[0,0,0]}` + `metadata`）。附录 A 说的"过不了 validate"**从未被实际验证过** | ⚠️ **待查**：identity transform 在 CityJSON 里是否合法、是否必须量化，**本地无法判定**（见下） |
| **4** | **P3 river accumulate 脚本** | git f534123 | 动机信只说 "water-flow analysis"，未点名 river accumulate，**风险最低** |

**⚠️ P4 的验证卡在哪（2026-09-21 实测记录）**

```
cjio validate 实际从未真正跑过：
  "Validation skipped: Python module 'cjvalpy' not installed"
→ 即附录 A 那句"过不了 cjio validate"是推断，不是实测结论

本地补齐的尝试也失败：
  pip install cjvalpy → ERROR: No matching distribution found（阿里云镜像无此包）

→ 正式做法：用官方网页校验器 https://validator.cityjson.org
  上传 04-cityjson-model/out.json 与 05-ifc2cityjson/output.city.json
  这一步能给权威答案，且不需要本地环境
```

**修订后的逐日安排（2026-09-21 三次重写：修正 9/22-9/25）**

| 日期 | 内容 |
|---|---|
| 9/21 一 | ✅ 已完成：缺陷 #1（`voxel_filter.py`）+ 论文 Hoppe 1992 |
| **9/22 二** | 早 1h：论文笔记收尾 ｜ 白天：**#4 Amsterdam 三张图**（M0 保命项）｜ **晚（收尾 1h）：给 `pointnet.py` 加落盘日志 → 启动训练，过夜跑** |
| **9/23 三** | 早 1h：**看训练结果 → 写进 `08-pointnet/README.md`**（或收尾）｜ 白天：**P5 补 `metadata`+`transform`** + 用网页校验器验 P4/P5 ｜ **收工前仓库应达到"可看"状态** |
| **9/24 四** | 早 1h：卫生项 #9 目录命名 ｜ 白天：**#5 `icp.py` 改 RMSE**（提前，因 #4 已挪回 9/22） |
| **9/25 五** | 早 1h：卫生项 #8 requirements ｜ 白天：**#6 建 `tests/` + 第一批测试**（**晚不写代码**） |
| **9/26 六** | 上午：**#7 真实 LAZ 跑通 ICP + 记 RMSE/耗时** → **M0 保命项验收**；下午：申请线（英语读者轮反馈收口）；晚：复盘 |
| 9/27 日 | 休息 |
| 9/28 一 | **M0.5 数学补丁轮开始**（白天）：Q5 三个几何量 + Q6 条件数 ｜ 早 1h：卫生项 #10 空文件清理 |
| 9/29 二 | M0.5：Q4 SVD 符号机制 + Q1 Rayleigh 商 ｜ 早 1h：卫生项 #11 `#` 前缀 |
| 9/30 三 | M0.5：Q2 共线机制 + Q3 `UΣVᵀ` 意义 ｜ 早 1h：卫生项 #2/#3 halfedge ｜ **9 月总验收** |

> **2026-09-21 三次修正说明**：上一版把 #4 挪到 9/24、9/22 全天排 P8，**是错的**。P8 的真正瓶颈是**几小时的机器时间**，不是人的工时——
> 而 `pointnet.py` 只 print 到 stdout，**加落盘日志是约 1h 的活，跑训练是过夜的活**，两者都不该占用 #4 的白天工时。
> 故改为：#4 留在 **9/22 白天**，P8 用 **9/22 晚启动、过夜跑、9/23 早收结果**；#5 与 #6 相应各前移一天（9/24、9/25），9/26 验收不变。

**P8 现成条件（2026-09-22 更新：数据已归位，训练已跑通）**

```
✅ CUDA 可用：torch 2.12.1+cu130（编译于 CUDA 13.0），RTX 5060 Ti，17.1 GB 显存
✅ 数据已复制到 08-pointnet\data\modelnet40\ModelNet40\（40 类 / 12,311 文件 / 9.3 GB）
   → provider.py 的相对路径现在从 08-pointnet\ 能直接解析，不必再 cd 到 ml\
✅ 日志已补：pointnet.py 每 epoch 追加一行 + 结束写汇总（best acc / best epoch / 总耗时 / GPU）
✅ 2026-09-22 晚实测：首轮 765 s（首次读取+采样 9843 个 .off），其后每轮约 9.2 s
   → 200 轮总计约 40 分钟；第 20-30 轮已到 acc 0.85 量级，与 README 的"约 50 epoch 入平台"吻合
⚠️ 日志格式小瑕疵：f.write 末尾缺换行符，全部 epoch 挤在一行 → 需补 '\n'
⚠️ 08-pointnet\data\ 已在 .gitignore 中，9.3 GB 不会进仓库（正确）
```

**被推迟的项及其新归属**

| 项 | 现在排到哪 |
|---|---|
| **#6 建 `tests/`** | **10/1（周四）** ← 从 9/25 让位 |
| **概率三块**（RANSAC 迭代推导 / 鲁棒核 / 最小二乘=MLE） | **10/2（周五）** |
| **P3 river accumulate** | 10/3 或更晚（优先级最低，动机信未点名） |
| **#8 requirements + #9 目录命名 + 生成物处理** | **10/3（周六）** |

> **⚠️ 取舍说明**：9/25 原排 `#6 建 tests/`，改为 `#5 icp.py 改 RMSE`。理由是 **#5 直接产出可展示的数字（RMSE），而 tests/ 是内部质量**——在"链接会被点开"的前提下，数字优先。
> `tests/` 不取消，已明确排到 **10/1**。

## 六之二、10 月 1-4 日（2026-09-22 补入：填掉原文档到 9/30 就断掉的空白）

> 空白原因：M0.5 只占 12-20h（3 个工作日），所以 9/30 之后文档里没有内容。
> 而这几天有 4 类事情在等：被推迟项、卫生轮剩余、TUD 提交准备、论文单价复评。

| 日期 | 上午/白天 | 早 1h |
|---|---|---|
| **10/1 四** | **#6 建 `tests/` + 第一批测试**（给已修的 `voxel_filter` / `icp` 补回归测试；含一条 `test_empty_input`——它能自动抓住 9/21 那个早退分支的坑） | 卫生项 #9 目录命名（**含 `07-voxel_filter` vs `07-Voxel_filter` 大小写不一致**） |
| **10/2 五** | **概率三块**：RANSAC 迭代次数推导 · 鲁棒核 · 最小二乘 = 高斯噪声下的 MLE（约 4h，闭合任务） | 卫生项 #8 `requirements.txt` + 解释器说明 |
| **10/3 六** | 上午：卫生轮收尾（生成物处理：8 张根目录 png / `out.json` / 5 个 `.bak` / 两个临时探针的去留）｜下午：申请线｜晚：复盘 | — |
| **10/4 日** | **休息** | — |
| **10/5 一** | ★ **论文单价复评**（本人定的两周评估点）：看这三周平均每篇多少小时 → 决定"每周 1 篇 / 60 篇"要不要改 | — |

**10 月的硬锚点（与上面的日常任务并行，且优先）**

```
10 月中    TUD 系统开放 → 当天提交（容量制先到先得）  ← 唯一不可推迟
           □ 开放日盯系统；新护照有效期须 ≥ 2028-03
10 月中下旬 A 线启动：M1 法向 + 曲率 + 全局定向（16 个月主线的真正起点）
```

> **本周余下（9/23-9/26）不受此节影响**，仍按上面的逐日表执行。

## 七、旧逐日表（已被上面的"修订后安排"取代，仅留档）

> **本节作废**。原表（9/21 首次重写）把 9/25 排成"建 `tests/`"、9/29 排成"补 86% 训练记录"，**未考虑"动机信已加仓库链接"这条依赖**。
> 上节"修订后的逐日安排"是当前唯一有效版本，差异主要有三处：
> ① 附录 A 四项从 10 月提前到 **9/22-9/23**（因为链接会被点开）；
> ② `tests/` 从 9/25 推迟到 10 月（让位给更紧急的证据类工作）；
> ③ 9/30 的验收范围改为"保命项 + 附录 A 三项（P8/P5/P4）"。

**（以下为原表，不再执行）**

| 日期 | 内容 |
|---|---|
| 9/19 六 晚 | **休息**（§4quater 约束 2） |
| 9/20 日 | **全天休息**（不可侵犯） |
| 9/21 一 | 早 1h：**论文第 1 篇 Hoppe 1992**；白天：缺陷 #1（`voxel_filter.py`） |
| 9/22 二 | 早 1h：论文笔记收尾；白天：缺陷 #4（三张图） |
| 9/23 三 | 早 1h：卫生项 #9 目录命名；白天：`icp.py` 改 RMSE（上） |
| 9/24 四 | 早 1h：卫生项 #8 requirements；白天：`icp.py` 改 RMSE（下）+ 合成数据验证 |
| 9/25 五 | 早 1h：卫生项 #10 空文件清理；白天：建 `tests/` + 第一批测试（**晚不写代码**） |
| 9/26 六 | 上午：真实 LAZ 跑通 ICP，记录 RMSE/耗时；下午：申请线；晚：复盘 |
| 9/27 日 | 休息 |
| 9/28 一 | **M0.5 数学补丁轮开始**：Q5 三个几何量 + Q6 条件数；早 1h：卫生项 #11 `#` 前缀 |
| 9/29 二 | M0.5：Q4 SVD 符号机制 + 附录 A 第 10 项（补 86% 训练记录）；早 1h：卫生项 #2 |
| 9/30 三 | M0.5：Q1 Rayleigh 商 + Q2 共线机制 + Q3 `UΣVᵀ` 几何意义；早 1h：卫生项 #3；**保命项验收** |

---

## 八、验收线（2026-09-21 修订）

**9/26（周六上午）—— M0 保命项验收**

```
□ voxel_filter.py 零报错 + 打印压缩率            ✅ 9/21 已完成
□ 三张图使用三种不同 scheme（目视确认）           9/24
□ icp.py 打印 RMSE / 迭代数 / 耗时              9/25
□ 真实 LAZ 上有可复现的 RMSE 与耗时数字          9/26
□ tests/ 至少 3 个测试能跑                       → 推迟到 10 月
```

**9/30（周三）—— 附录 A 证据项验收**

```
□ P8  86% 的训练记录可核实（或被正文修正）        9/22
□ P5  output.city.json 含 metadata + transform   9/23
□ P4  经 https://validator.cityjson.org 判定，并留下结论  9/23
□ P3  river accumulate 脚本                        → 推迟（优先级最低）
```

> 若 9/30 未全部完成：**砍范围不追债**（§0 纪律）。优先保：P8 > P5 > #4 > #5。
